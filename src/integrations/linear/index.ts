/**
 * Linear Integration
 *
 * Fetches issues from Linear workspaces.
 * Uses the Linear CLI for authentication or API key.
 */

export { LinearIntegration } from './source.js';
export { LinearOAuthProvider, linearProvider } from './auth.js';
